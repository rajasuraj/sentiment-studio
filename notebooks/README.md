# Notebooks (optional)

The assessment pipeline runs through the **FastAPI API**, CLI scripts (e.g. `scripts/prepare_raw_datasets.py`), and automated **pytest** tests. Jupyter notebooks are not required for training or inference.

You may add exploratory notebooks here for EDA; keep heavy training on the API or CLI so paths stay aligned with `src/config/*.yaml`.
